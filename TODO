TODO list

  * update license
  * quasinewton_search -> qnsearch
  * c++: wrap library with namespace::liquid
  * remove any trace of Doxygen from code
  * build: clean up make-included dependencies
  * add method to compute r^T * r of [N x 1] vector
  * add method to compute Cholesky factor, R, of positive-definite
    matrix A, where R^T * R = A
  * validate modem_modulate() input symbol size

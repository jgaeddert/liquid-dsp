TODO list

build:
  * update license
  * c++: wrap library with namespace::liquid
  * remove any trace of Doxygen from code
  * specify liquid-dsp library in fprintf(stderr...) messages
doc:
  * full flexframe tutorial (configuration, expectations, etc.)
framing:
  * fix flexframesync_setprops() to actually set properties of objects
    - need to create objects first
    - otherwise bad things happen
matrix:
  * add method to compute r^T * r of [N x 1] vector
  * fix matrix_pivot() function (see sandbox/simplex_test.c)
optim:
  * quasinewton_search -> qnsearch
